[package]
name = "rook-native"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
blake3 = { version = "1.8.2", default-features = false, features = ["pure"] }
rook-core = { path = "../rook-core" }
