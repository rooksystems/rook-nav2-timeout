[package]
name = "rook-host"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
anyhow = "1"
blake3 = "1"
rook-core = { path = "../rook-core" }
wasmtime = { version = "38", default-features = false, features = ["cranelift", "gc-null", "runtime", "std", "threads"] }
